import { MouseOutsideDirective } from './mouse-outside.directive';

describe('MouseOutsideDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseOutsideDirective();
    expect(directive).toBeTruthy();
  });
});
